# DocFormato

Convierte una foto de un documento en Word editable, conservando párrafos, viñetas y alineación. Todo ocurre en el navegador.

Abrir `index.html` desde un servidor local (no como archivo suelto) para que el motor de lectura funcione.
